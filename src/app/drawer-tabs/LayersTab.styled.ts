import { styled } from '@mui/material';
import { px } from '../../utils/ui';

const Circle = styled('div')`
  width: ${px(16)};
  height: ${px(16)};
  border-radius: 50%;
`;

export default {
  Circle
};
